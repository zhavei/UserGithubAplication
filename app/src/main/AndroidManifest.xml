<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.syafei.usergithubaplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/github_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/github_splash"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserGithubAplication">
        <activity
            android:name=".ui.onboarding.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.details.UserDetailActivity"/>
    </application>

</manifest>